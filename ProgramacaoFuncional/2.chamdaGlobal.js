let fixedValue = 4;

function incrementer() {
  // Altere apenas o código abaixo desta linha
  let valueAlterado = fixedValue + 1
  return valueAlterado

  // Altere apenas o código acima desta linha
}
console.log(incrementer())
console.log(fixedValue)

// clg => console.log()